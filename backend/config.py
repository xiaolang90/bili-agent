#!/usr/bin/env python3
"""
配置文件 - 存储所有常量配置

此模块包含应用的所有配置常量，包括：
- 服务器配置
- 上传限制
- 默认认证信息
- 数据库配置
"""

import os
import tempfile

# =============================================================================
# 服务器配置
# =============================================================================

# Flask 服务器端口（与原项目 5000 区分）
SERVER_PORT = 5001

# 服务器主机
SERVER_HOST = 'localhost'

# 调试模式
DEBUG_MODE = True

# =============================================================================
# 上传配置
# =============================================================================

# 临时文件夹路径 - 用于存储上传的临时文件
UPLOAD_FOLDER = tempfile.gettempdir()

# 允许上传的视频文件扩展名
ALLOWED_EXTENSIONS = {'mp4', 'mkv', 'mov', 'flv', 'avi', 'webm'}

# 最大文件大小限制：16GB
MAX_FILE_SIZE = 16 * 1024 * 1024 * 1024

# =============================================================================
# 数据库配置
# =============================================================================

# SQLite 数据库文件路径
DB_FILE = os.path.join(
    os.path.dirname(os.path.dirname(__file__)), 
    'bilibili_uploader_refactored.db'
)

# =============================================================================
# 视频封面提取配置
# =============================================================================

# ffmpeg 可执行文件路径(请改成你自己的)
FFMPEG_PATH = '/opt/local/bin/ffmpeg'

# 封面提取超时时间（秒）
COVER_EXTRACT_TIMEOUT = 30

# 默认从视频第几秒截取封面
COVER_TIME_OFFSET = 10

# =============================================================================
# SSE 配置
# =============================================================================

# SSE 客户端队列最大大小
SSE_QUEUE_MAXSIZE = 100

# SSE 全局队列最大大小
SSE_GLOBAL_QUEUE_MAXSIZE = 200

# SSE 心跳超时时间（秒）
SSE_HEARTBEAT_TIMEOUT = 30

# =============================================================================
# 上传历史配置
# =============================================================================

# 自动清理历史记录 - 保留的最大记录数
MAX_HISTORY_KEEP = 100

# 默认分页大小
DEFAULT_PAGE_SIZE = 10
